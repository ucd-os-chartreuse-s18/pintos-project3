#ifndef SWAP_H
#define SWAP_H

/*
We will need to use the pintos disk utility to create a swap partition called
`swap.dsk` and load that in when we run pintos.

If we run out of swap space, panic the kernel. When loading a page back into
memory, we will need to free the memory that is used in the swap space.
*/

#endif
